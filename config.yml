# Параметри, які можна змінювати для нового експерименту.
#
# Формат часу: YYYY-MM-DD HH:MM:SS
# YYYY = рік, MM = місяць, DD = день.
#
# Кінець періоду обчислюється автоматично:
# study.data_start + study.data_years.
# Права межа не включається.
study:
  # Binance використовується лише для початкового порівняння з Bybit.
  candidate_exchange: "binance"
  primary_exchange: "bybit"
  reference_exchange: "bitstamp"
  market_type: "spot"
  interval: "1h"
  price_field: "close"
  timezone: "UTC"
  data_start: "2021-07-06 00:00:00"
  data_years: 5

# Останній рік перед тестом призначено для внутрішньої перевірки.
# Фінальний тест охоплює наступний, повністю відкладений рік.
evaluation:
  validation_years: 1
  test_years: 1

# Параметри описового аналізу, який передує вибору моделі.
analysis:
  mean_max_lag: 48
  dependence_max_lag: 168
  rolling_window_hours: 720
  rolling_step_hours: 168
  histogram_bins: 120
  normal_qq_points: 501
  relationship_groups: 10


exchanges:
  binance:
    name: "Binance"
    symbol: "BTCUSDT"
    base_currency: "BTC"
    quote_currency: "USDT"
    documentation_url: "https://github.com/binance/binance-public-data"
    archive_base_url: "https://data.binance.vision/data/spot/monthly/klines"
    rest_endpoint: "https://data-api.binance.vision/api/v3/klines"
  bybit:
    name: "Bybit"
    symbol: "BTCUSDT"
    base_currency: "BTC"
    quote_currency: "USDT"
    available_start: "2021-07-05 12:00:00"
    documentation_url: "https://bybit-exchange.github.io/docs/v5/market/kline"
    endpoint: "https://api.bybit.com/v5/market/kline"
  bitstamp:
    name: "Bitstamp"
    symbol: "btcusd"
    base_currency: "BTC"
    quote_currency: "USD"
    documentation_url: "https://www.bitstamp.net/api/#tag/Market-info/operation/GetOHLCData"
    endpoint: "https://www.bitstamp.net/api/v2/ohlc"

runtime:
  workers: 2
  # Керує лише явним запуском scripts/ensure_project_data.R.
  # Quarto render завжди перевіряє готові локальні дані без їх оновлення.
  auto_download_data: true
